# 📄 Список доменов для ручной блокировки

Этот репозиторий содержит **лист доменов для самостоятельной блокировки**.
Он предназначен для использования в роутерах, DNS-фильтрах и системах сетевой фильтрации, где поддерживается импорт внешних списков доменов.

Список ведётся в простом текстовом формате и может подключаться как **внешний (remote) источник**.

---

## 🔗 Raw-файл со списком доменов

Актуальная версия списка доступна по прямой ссылке:

```
https://raw.githubusercontent.com/Pixmint/allow-domains/refs/heads/main/all-list.lst
```

⚠️ При подключении обязательно указывайте полный URL с протоколом (`https://`).

### Загрузка вручную

```bash
curl -L -o all-list.lst "https://raw.githubusercontent.com/Pixmint/allow-domains/refs/heads/main/all-list.lst"
```

---

## ⚙️ Настройка в Podkop

Podkop поддерживает подключение **внешних списков доменов (Remote Domains Lists)** с GitHub или любого сайта.

### Путь в интерфейсе

**Podkop → Секции → Внешние списки доменов**

### Что указать

В поле источника вставьте ссылку на raw-файл:

```
https://raw.githubusercontent.com/Pixmint/allow-domains/refs/heads/main/all-list.lst
```

После сохранения список будет автоматически загружаться и обновляться.

---

## 🧾 Формат файла

Файл представляет собой обычный текстовый список доменов:

* **один домен — одна строка**
* пустые строки игнорируются
* строки, начинающиеся с `//`, считаются комментариями

Пример:

```
// комментарий
example.com
sub.example.com
```

---

## 🔧 Ручное использование и другие системы

Список также можно использовать вручную в:

* настройках роутера (блокировка доменов);
* DNS-фильтрах и sinkhole-решениях;
* adblock / firewall / proxy;
* собственных скриптах обновления.

Пример: загрузить файл и импортировать его в систему фильтрации или обработать автоматически.

---

## ➕ Предложение новых доменов

**Новые домены предлагаются через Issues.**

Pull Request создавать не обязательно — достаточно Issue с:

* доменом;
* причиной добавления;
* при возможности — источником или пояснением.

После проверки домен будет добавлен в список.

---

## 🔁 Обновление списка

* При использовании raw-ссылки обновления применяются автоматически
* При ручной загрузке файл нужно обновлять вручную
